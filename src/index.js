import React from "react"
import ReactDOM from "react-dom"
import './index.css'
import App from './app' //we can import apps/components and use them


ReactDOM.render(<App />, document.getElementById('root')) //This method renders an element to an id using the DOM